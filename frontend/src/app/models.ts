export interface ApiResponse<T>{success:boolean;message:string;data:T}export interface PagedResult<T>{items:T[];page:number;pageSize:number;total:number;totalPages:number}export interface AuthResult{token:string;expiresAt:string;userId:number;userName:string;role:string}export interface AdImage{id:number;url:string;sortOrder:number;isPrimary:boolean}export interface Ad{id:number;title:string;description:string;price:number;status:string;userId:number;categoryId:number;regionId:number;createdAt:string;images:AdImage[];attributes:Record<string,string>}export interface Category{id:number;name:string;children:Category[]}export interface Conversation{id:number;adId:number;buyerUserId:number;sellerUserId:number;lastMessageAt?:string;messages?:Message[]}export interface Message{id:number;conversationId:number;senderUserId:number;body:string;createdAt:string;attachments:{url:string}[]}
