import{Component,inject,OnInit}from'@angular/core';import{CurrencyPipe}from'@angular/common';import{FormBuilder,ReactiveFormsModule}from'@angular/forms';import{RouterLink}from'@angular/router';import{ApiService}from'../../core/services/api.service';import{Ad,PagedResult}from'../../models';import{environment}from'../../../environments/environment';
@Component({standalone:true,imports:[ReactiveFormsModule,RouterLink,CurrencyPipe],template:`<h1>أحدث الإعلانات</h1><form class="row g-2 mb-4"[formGroup]="filters"(ngSubmit)="load()"><div class="col-md-5"><input class="form-control"placeholder="ابحث..."formControlName="search"></div><div class="col"><input class="form-control"type="number"placeholder="أقل سعر"formControlName="minPrice"></div><div class="col"><input class="form-control"type="number"placeholder="أعلى سعر"formControlName="maxPrice"></div><div class="col-auto"><button class="btn btn-primary">بحث</button></div></form><div class="row g-3">@for(ad of result?.items;track ad.id){<div class="col-md-4"><a class="card ad-card text-decoration-none text-dark h-100"[routerLink]="['/ads',ad.id]"><img class="card-img-top object-cover"height="220"[src]="image(ad)"><div class="card-body"><h5>{{ad.title}}</h5><b class="text-success">{{ad.price|currency:'SAR':'symbol':'1.0-0'}}</b></div></a></div>}@empty{<p>لا توجد نتائج.</p>}</div>@if(result&&result.totalPages>1){<div class="mt-4"><button class="btn btn-outline-primary"[disabled]="page===1"(click)="page=page-1;load()">السابق</button><span class="mx-3">{{page}} / {{result.totalPages}}</span><button class="btn btn-outline-primary"[disabled]="page===result.totalPages"(click)="page=page+1;load()">التالي</button></div>}`})export class AdListComponent implements OnInit{private api=inject(ApiService);private fb=inject(FormBuilder);result?:PagedResult<Ad>;page=1;filters=this.fb.group({search:[''],minPrice:[null as number|null],maxPrice:[null as number|null]});ngOnInit(){this.load()}load(){this.api.get<PagedResult<Ad>>('ads',{page:this.page,pageSize:12,...this.filters.value}).subscribe(r=>this.result=r.data)}image(a:Ad){const u=a.images?.[0]?.url;return u?(u.startsWith('http')?u:environment.assetUrl+u):'https://placehold.co/600x400?text=Ad'}}
