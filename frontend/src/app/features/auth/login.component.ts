import{Component,inject}from'@angular/core';import{FormBuilder,ReactiveFormsModule,Validators}from'@angular/forms';import{Router,RouterLink}from'@angular/router';import{AuthService}from'../../core/services/auth.service';
@Component({standalone:true,imports:[ReactiveFormsModule,RouterLink],template:`<div class="card mx-auto p-4"style="max-width:480px"><h2>تسجيل الدخول</h2><form [formGroup]="form"(ngSubmit)="submit()"><label class="form-label">البريد</label><input class="form-control mb-3"type="email"formControlName="email"><label class="form-label">كلمة المرور</label><input class="form-control mb-3"type="password"formControlName="password">@if(error){<div class="alert alert-danger">{{error}}</div>}<button class="btn btn-primary w-100"[disabled]="form.invalid||loading">دخول</button></form><a class="mt-3"routerLink="/auth/register">إنشاء حساب</a></div>`})export class LoginComponent{private fb=inject(FormBuilder);private auth=inject(AuthService);private router=inject(Router);loading=false;error='';form=this.fb.nonNullable.group({email:['',[Validators.required,Validators.email]],password:['',Validators.required]});submit(){if(this.form.invalid)return;this.loading=true;this.auth.login(this.form.getRawValue()).subscribe({next:()=>void this.router.navigate(['/']),error:e=>{this.error=e.error?.message??'تعذر تسجيل الدخول';this.loading=false}})}}
