import{Component,inject,OnInit}from'@angular/core';import{ApiService}from'../../core/services/api.service';import{Category}from'../../models';@Component({standalone:true,template:`<h1>التصنيفات</h1><div class="row">@for(c of categories;track c.id){<div class="col-md-4"><div class="card p-3 mb-3"><h4>{{c.name}}</h4><ul>@for(ch of c.children;track ch.id){<li>{{ch.name}}</li>}</ul></div></div>}</div>`})export class CategoriesComponent implements OnInit{private api=inject(ApiService);categories:Category[]=[];ngOnInit(){this.api.get<Category[]>('categories').subscribe(r=>this.categories=r.data)}}
