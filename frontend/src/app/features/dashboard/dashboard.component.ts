import{Component,inject}from'@angular/core';import{AsyncPipe}from'@angular/common';import{RouterLink}from'@angular/router';import{AuthService}from'../../core/services/auth.service';@Component({standalone:true,imports:[AsyncPipe,RouterLink],template:`<h1>لوحة التحكم</h1>@if(auth.user$|async;as u){<div class="card p-4"><h3>مرحباً {{u.userName}}</h3><p>الدور: {{u.role}}</p><div class="d-flex gap-2"><a class="btn btn-primary"routerLink="/ads/new">إعلان جديد</a><a class="btn btn-outline-primary"routerLink="/chat">المحادثات</a>@if(u.role==='Admin'){<a class="btn btn-danger"routerLink="/admin">الإدارة</a>}</div></div>}`})export class DashboardComponent{auth=inject(AuthService)}
